name: Run Appium Test Every 15 Minutes

on:
  #schedule:
    #- cron: '*/15 * * * *'  # Cron expression to trigger every 15 minutes
  
  workflow_dispatch:
<<<<<<< HEAD
    
=======
>>>>>>> 58e2820faf85d89dce13357acac1d3cb005f3f46

jobs:
  build-and-test:
    runs-on: macos-latest

    steps:
<<<<<<< HEAD
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Set up Appium
        run: |
          npm install -g appium@2
          appium -v
          appium &>/dev/null &

      - name: Set up UIAutomator2
        run: |
          appium driver install uiautomator2
  
      - name: Set up Emulator
        run: |
          echo no | $ANDROID_HOME/tools/bin/avdmanager create avd -n emulatortest2 -k "system-images;android-29;google_apis;x86"
          $ANDROID_HOME/emulator/emulator -avd emulatortest2 -no-audio -no-window &
          android-wait-for-emulator
          adb devices

      - name: Run Appium Tests
        run: mvn -Dtest=appium.MobileTesting.Logintest test
          
      #- name: Compile Appium test
        #run: javac -cp "src/test/java" src/test/java/appium/MobileTesting/Logintest.java

      #- name: Run Appium test
       # run: java -cp "src/test/java" appium.MobileTesting.Logintest
=======
       - name: Set up Java 11
         uses: actions/setup-java@v4
         with:
            distribution: 'temurin'
            java-version: '21'
            
       - name: Checkout code
         uses: actions/checkout@v3
        
       - name: Set up Appium
         run: |
           npm install -g appium@2
           appium -v
           appium &>/dev/null &

       - name: Set up UIAutomator2
         run: |
          appium driver install uiautomator2
  
       - name: run tests
         uses: reactivecircus/android-emulator-runner@v2
         with:
          api-level: 29
          script: mvn clean test
>>>>>>> 58e2820faf85d89dce13357acac1d3cb005f3f46
